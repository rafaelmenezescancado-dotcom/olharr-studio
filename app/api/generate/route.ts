import { NextRequest, NextResponse } from "next/server";import { ORCHESTRATOR_PROMPT } from "@/app/lib/personas";export async function POST(req: NextRequest) {  try {    const body = await req.json();    const apiKey = process.env.GEMINI_API_KEY;    if (!apiKey) {      return NextResponse.json(        { error: "GEMINI_API_KEY não configurada. Adicione no .env.local" },        { status: 500 }      );    }    // Build the briefing from form data    const briefing = buildBriefing(body);    // Call Gemini API for the orchestrator    const response = await fetch(      `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=${apiKey}`,      {        method: "POST",        headers: { "Content-Type": "application/json" },        body: JSON.stringify({          contents: [            {              role: "user",              parts: [                {                  text: `${ORCHESTRATOR_PROMPT}\n\n--- BRIEFING DO EVENTO ---\n${briefing}\n\nGere o JSON completo do moodboard e storyboard.`,                },              ],            },          ],          generationConfig: {
